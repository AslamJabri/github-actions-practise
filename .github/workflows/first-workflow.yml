name: my first workflow
on: push
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Welcome Message
          run: echo "Hello form github actions flow"
        - name: Git repo checkout
          uses: actions/checkout@v6
        - name: list files
          run: ls
        - name: show directory
          run: pwd